import java.awt.* ; 

import javax.swing.JOptionPane;

public class Quiz {
	
	public void questionare() {
		int y = 0 ; 
		int n = 0 ; 
		int mes1 , mes2 , mes3 , mes4 , mes5 ; 
		// Breanna came up with the questions to ask to determine the joke
		// Group decided the order it will go into 
		// Doryan, Cameron in process of of wokring on quiz implementation. 
		mes1 = JOptionPane.showConfirmDialog(null, "Are you in a happy mood?") ;
		mes2 = JOptionPane.showConfirmDialog(null, "Do you like to hear short or long jokes?") ; 
		mes3 = JOptionPane.showConfirmDialog(null, "Are you a male or female?") ;
		mes4 = JOptionPane.showConfirmDialog(null, "Are you above the age of 30? ") ;
		mes5 = JOptionPane.showConfirmDialog(null, "Do you have children?") ; 
		//
		JOptionPane.showConfirmDialog(null, mes1) ;
		JOptionPane.showConfirmDialog(null, mes2) ;
		JOptionPane.showConfirmDialog(null, mes3) ;
		JOptionPane.showConfirmDialog(null, mes4) ;
		JOptionPane.showConfirmDialog(null, mes5) ;
		// counts the responses the user gives and will show a different category for how many yes's and no's counted.
		//could make separate joke classes , 
		//if else statments 
		//
		// 
		if(mes1 = ) {
		y++ ;
		}
		else {
			n++ ;
		}
		if(mes2 == yes) {
			y++ ;
		}
		else {
			n++ ;
		}
		if(mes3 == yes) {
			y++ ;
		}
		else {
			n++ ;
		}
		if(mes4 == yes) {
			y++ ;
		}
		else {
			n++ ; 
		}
		if(mes5 == yes) {
			y++ ; 
		}
		else {
			n++ ; 
		}
		
		
		
		
		
	}
}
